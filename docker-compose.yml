version: "3.9"  # optional since v1.27.0
services:
  web:
    build: 
      context: .
      dockerfile: Dockerfile
    image: eendorsement-auth-api:1.0
    extra_hosts:
       - "outside:${HOST_IP}"
    ports:
      - "80:9000"
    restart: always
    networks:
      - eendorsement
networks:
  eendorsement: